[project]
name = "resource-gate-integrations"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot>=21.0",
    "modal>=0.64",
    "runpod>=1.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"
